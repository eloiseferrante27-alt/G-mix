# Règles supplémentaires spécifiques au dossier MVP/
# (les règles globales sont dans le .gitignore à la racine du dépôt)

# Lock files (on versionne uniquement package.json, pas les locks générés)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Cache outils
.eslintcache
.cache
.parcel-cache
.node_repl_history

# Ancienne structure (MVP/apps/) — remplacée par MVP/frontend/
apps/
