# Django
SECRET_KEY=change-me-in-production
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,G-mix

# PostgreSQL — local G-mix server
DB_NAME=G-mix_database
DB_USER=postgres
DB_PASSWORD=1
DB_HOST=localhost
DB_PORT=5432

# CORS — allow the Next.js frontend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003

# JWT
ACCESS_TOKEN_LIFETIME_MINUTES=60
REFRESH_TOKEN_LIFETIME_DAYS=7

# Anthropic (AI scenario generation)
ANTHROPIC_API_KEY=sk-ant-...
